import { NavItem } from '@/types'

export const navItems: NavItem[] = [
  {
    title: 'Dashboard',
    url: '/',
    icon: 'dashboard',
    isActive: true,
    shortcut: ['d', 'd'],
    items: [],
  },
]
